1. FileCard WIP is the only real blocker I found. Commit `1914357` adds new imports in `packages/renderer/src/cards/FileCard.tsx:3`-`5` and `:267` (`mammoth`, `papaparse`, `xlsx`, `pptx-preview`), but the committed dependency list at `packages/renderer/package.json:16`-`27` does not include any of them. The current dirty worktree has uncommitted package/lock changes that make `pnpm --filter @f-mark/renderer build` pass, but a clean checkout of `1914357` will not resolve those modules. Either commit the dependency + lockfile changes with this WIP or drop the WIP from Phase 2. Also, `FileCard` accepts and displays `comments` at `FileCard.tsx:316`-`320` and `:405`-`410`, but `EventCard` does not pass that prop for file events (`packages/renderer/src/cards/EventCard.tsx:99`-`100`), so file comment counts stay at zero.

2. Helper-boundary integrity is clean. `getProseRole` is explicitly shape-only and only maps legacy `target` when `append_to` is absent (`packages/shared/src/proseRoles.ts:30`-`63`); `isNamedAnchor` and `getCommentTarget` only delegate to that role helper (`packages/shared/src/blocks.ts:38`-`52`). None of these helpers walks `supersedes`. Source grep found no direct `payload.target` consumer outside the intended compatibility layer (`prose.ts`, `proseValidate.ts`, `routes/events.ts`, shared helper/types). Other `.target` hits are DOM events, flow edges, store `commentTarget`, or tests/fixtures.

3. Named/comment consumer migration looks complete. `aggregate` uses `getProseRole`, `getCommentTarget`, and `isNamedAnchor` for comments, feed slices, and `named` (`packages/renderer/src/state/aggregate.ts:15`-`18`, `:42`-`63`). `RightNamed`, the old `Named` panel, and Cmd-K read `aggregate(events).named`, while the named-only log filter calls `isNamedAnchor` directly (`packages/renderer/src/popovers/log-filter-types.ts:142`-`147`). I did not find a remaining named-rail entry that would admit named sub-blocks.

4. The prose write normaliser and validator are wired correctly. `normaliseProseBody` rejects mixed legacy/new comment shape (`packages/kernel/src/routes/events.ts:28`-`37`), maps legacy `target` to `append_to + mode: "comment" + lines` before serialization (`:40`-`46`), and the route calls `validateProseFrontmatter` before `serializeProse` (`:148`-`172`). Tests cover legacy POST -> new-shape disk file (`packages/kernel/tests/routes/events.test.ts:91`-`111`), target-without-lines (`:116`-`143`), target + append_to -> 400 (`:147`-`167`), and validator rules (`packages/kernel/tests/events/proseValidate.test.ts:95`-`265`). `additionalProperties: false` is present on the prose body schema (`routes/events.ts:101`-`143`); note the current tests document Fastify stripping unknown properties rather than hard-400ing them (`events.test.ts:250`-`256`).

5. The 7 renderer failures are WIP coupling, not Phase 2 schema regressions. Current `pnpm --filter @f-mark/renderer test` fails: `tests/compose.test.tsx` mode-button tests at `:143`/`:164` because the dirty `ModeBar` now has only the Name pill (`packages/renderer/src/compose/ModeBar.tsx:20`-`22`); send-button tests at `compose.test.tsx:397`, `:430`, `:441` because dirty `SendButton` now renders `.primary-action` (`packages/renderer/src/compose/SendButton.tsx:98`-`105`) instead of `.send-cluster`/`.send-btn`; and overlay-routing tests at `tests/overlays/comment-thread.test.tsx:92` and `:249` because dirty `RightPanel` always renders tabs and only switches to the comments tab (`packages/renderer/src/shell/RightPanel.tsx:32`-`40`). At committed `1914357`, `RightPanel` still has the overlay branch, and `ModeBar`/`SendButton` still match those test expectations. Kernel tests pass in the current checkout: `71` files, `455` tests.

# v0.4 Browser-Driven Smoke Checklist

Real end-to-end verification via Playwright MCP. Every line must be marked PASS, FAIL, or BLOCKED — with the actual observed behavior.

## Setup

- [ ] Build the renderer + kernel: `pnpm -r build`
- [ ] Start kernel in a fresh temp project: `node packages/kernel/bin/f-mark.js --port 17900 --password fmark-test` (token auth — same as a normal user)
- [ ] Read the printed URL with token query param
- [ ] Open the URL in the headless browser; confirm 200 + the F-Mark shell renders

## A. Sanity & state (no kernel interaction)

- [ ] A1 Top bar renders without console errors at first load
- [ ] A2 Settings modal opens via gear icon
- [ ] A3 Command palette opens via ⌘K
- [ ] A4 Existing sessions list renders if any
- [ ] A5 Create-new-session modal opens via the "+" in the session selector

## B. Env probe (read-only, no process API required)

- [ ] B1 Env probe banner renders OR is absent depending on tmux/runtime PATH
- [ ] B2 Settings → Env Probe panel shows tmux version + runtime detection + installer
- [ ] B3 Settings → Env Probe "Re-probe" button triggers a fresh fetch (intercept network)
- [ ] B4 Banner shows correct copyable install command for the detected installer

## C. + Button (the failing flow per user report)

- [ ] C1 + button is visible in the top bar chip strip
- [ ] C2 Clicking + opens a dropdown with: Claude, Codex, Gemini, Terminal, Manage runtimes…
- [ ] C3 Runtimes missing from PATH are disabled with a tooltip
- [ ] C4 Click + → Claude → ACTUAL NETWORK CALL TO `POST /managed-agents/spawn` (intercept and capture)
- [ ] C5 The response is JSON (not HTML — the user's reported failure mode)
- [ ] C6 On success, a new AgentChip appears in the strip immediately (no reload)
- [ ] C7 The chip shows the participant name (not just the id)
- [ ] C8 The chip's presence dot reflects the kernel-reported `hooks_status` (hook-not-installed when missing)
- [ ] C9 If hooks missing, Hook Install modal opens with copyable snippet for the runtime
- [ ] C10 Hook Install modal Copy button copies the actual snippet to clipboard
- [ ] C11 Hook Install modal Close button dismisses the modal
- [ ] C12 Spawn for Codex same flow (with TOML snippet)
- [ ] C13 Spawn for Gemini same flow (with manual-stream note, no hook snippet)
- [ ] C14 Click + → Terminal → spawns a terminal chip, appears immediately
- [ ] C15 Click + → Manage runtimes → opens Settings modal scrolled to Runtimes panel

## D. AgentChip + menu (managed agents)

- [ ] D1 Click on a managed AgentChip opens the action menu anchored to the chip
- [ ] D2 Menu shows the participant name in the header
- [ ] D3 Rename inline edit works (PATCH /participants/:id) — or note if it's a v0.5 stub
- [ ] D4 Send /compact button (managed + online) triggers `POST /managed-agents/:id/command` with `{type:"slash", command:"compact"}`
- [ ] D5 Send /compact is disabled with tooltip when state is not online
- [ ] D6 Slash submenu (clear/resume) works
- [ ] D7 Interrupt sends `{type:"interrupt"}` → confirm 200 response
- [ ] D8 Interrupt is disabled when offline/pane-dead
- [ ] D9 Send a message inline input sends `{type:"message", text}` after Enter
- [ ] D10 Open terminal opens the TerminalOverlay
- [ ] D11 Reconnect option shows ONLY for offline/pane-dead — opens ReconnectModal
- [ ] D12 Show last failure / logs option works (or note it's a stub)
- [ ] D13 Say goodbye triggers DELETE confirmation flow with one-time confirm-token
- [ ] D14 After Say goodbye, the chip disappears and the tmux session is killed (verify via `tmux ls`)
- [ ] D15 Menu closes on outside click and Escape

## E. AgentChip + menu (un-managed agents)

- [ ] E1 An un-managed agent participant (registered via the old POST /participants/register, no tmux-session pointer) appears as an AgentChip in the chip strip
- [ ] E2 The chip shows offline state dot if no recent ping
- [ ] E3 Menu shows: Rename, Reconnect (when offline)
- [ ] E4 Menu HIDES Send /compact, Slash submenu, Interrupt, Send-a-message, Open-terminal, Say-goodbye (because no tmux session)
- [ ] E5 Reconnect opens ReconnectModal with the rendered `/guide?agent_id=...&session_id=...&runtime_id=...`

## F. Terminal chips

- [ ] F1 Each terminal session shows as a TerminalChip with label
- [ ] F2 Click on TerminalChip opens the TerminalOverlay
- [ ] F3 TerminalOverlay shows the live tmux pane via /ws/pane (snapshot + streaming)
- [ ] F4 Typing in the overlay sends keystrokes to tmux (verify via `tmux capture-pane`)
- [ ] F5 Closing the overlay leaves the tmux session alive
- [ ] F6 Killing the terminal via menu kills the tmux session

## G. Presence dynamics

- [ ] G1 Set up: spawn a managed agent, install its hook manually, run the agent
- [ ] G2 On first hook fire, presence dot flips green (online) — observe via WS
- [ ] G3 After 60s of silence, dot flips amber (stale)
- [ ] G4 After 10m, dot flips gray (offline)
- [ ] G5 Kill the agent process; on next tracker tick, dot flips to pane-dead
- [ ] G6 "Agent thinking…" pill in top bar shows ONLY when at least one agent has presence online/stale

## H. Pane WS fan-out

- [ ] H1 Open the TerminalOverlay in two browser tabs against the same kernel + same session
- [ ] H2 Type in tab 1 — output appears in tab 2's xterm
- [ ] H3 Close tab 1 — tab 2 continues working

## I. Reconcile on restart

- [ ] I1 Spawn 2 managed agents + 1 terminal
- [ ] I2 `kill -9` the kernel process
- [ ] I3 Confirm `tmux ls` still shows all 3 sessions
- [ ] I4 Restart kernel; confirm chip strip restores the 3 entries
- [ ] I5 Confirm presence reflects: stale (waiting for next ping) for live agents

## J. Security gates

- [ ] J1 Kernel started with --no-auth (no allow-flag): POST /managed-agents/spawn returns 404 with documented error message
- [ ] J2 Kernel started with --no-auth --allow-process-api-no-auth: spawn returns 200 + banner shows red warning
- [ ] J3 With token auth + cookie session: POST /managed-agents/terminal from a foreign Origin header returns 403
- [ ] J4 With token auth + cookie session: POST without Origin returns 403
- [ ] J5 DELETE /managed-agents/:id without confirm token returns 403

## K. The reported bug (HTML response to spawn)

This is the user's actual hit. Reproduce explicitly:
- [ ] K1 Kernel running in DEFAULT mode (token auth, no special flags)
- [ ] K2 Browser loads UI via the printed URL with token in query string
- [ ] K3 Click + → Claude
- [ ] K4 Capture the actual network request URL + headers + response Content-Type + response body
- [ ] K5 EXPECTED: 200 JSON. ACTUAL: <observe and record>

## L. Settings panels

- [ ] L1 Settings → Runtimes panel shows three default runtimes
- [ ] L2 Save/Edit/Remove are visually disabled (read-only in v0.4) with the note explaining
- [ ] L3 Settings → Hook Status panel shows per-runtime status for an existing managed participant
- [ ] L4 "Show install instructions" button opens HookInstallModal
- [ ] L5 Settings → Env Probe panel shows current probe + Re-probe button works

## M. Top bar non-regressions

- [ ] M1 Existing user-typed messages still send (Compose still works)
- [ ] M2 Existing event feed still renders (don't break v0.3.0 path)
- [ ] M3 View toggle (Everything/Document/Conversation) still works
- [ ] M4 Theme switcher still works
- [ ] M5 Right-side participants stack now shows users only (no orphan agent avatars)

## N. v0.3.0 hook path regression

- [ ] N1 Start kernel, register an agent participant manually, install the auto-stream hook in `~/.claude/settings.json`, run claude in a separate terminal
- [ ] N2 Send a prompt; confirm the assistant turn flows into the F-Mark feed (auto-stream event posts)
- [ ] N3 Confirm presence ping endpoint was hit (network capture or kernel log)

## Findings record

Record each failure verbatim with: section, item, observed behavior, kernel log snippet, network response.
